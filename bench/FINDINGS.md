# Workstream A Findings

Status: **not yet run.** This file is a template — fill it in from an actual GPU run, don't
hand-edit numbers in. See `bench/README.md` for how to reproduce each section.

Every number below must be tied to a GPU model. Launch overhead as a fraction of total time
gets *worse* on faster hardware (GEMM time shrinks, fixed per-kernel launch cost doesn't), so
the same model can be, say, 25% launch-bound on an A100 and 40% launch-bound on an H100 — and
that gap moves in PyMantle's favor. Never report a ratio without the hardware next to it.

## Environment

- GPU: _(fill in — `torch.cuda.get_device_name()`)_
- CUDA version: _(fill in)_
- PyTorch version: _(fill in)_
- SpikingJelly version: _(fill in)_
- Date:

## 1. Single model, four-number breakdown

One model (MLP-SNN), one config, both frameworks, via `profile_kernels.sh` + `parse_profile.py`.

| category      | PyTorch (naive loop) | SpikingJelly (fused) |
|---------------|----------------------|------------------------|
| GEMM          | _%_                  | _%_                     |
| elementwise   | _%_                  | _%_                     |
| launch gaps   | _%_                  | _%_                     |
| allocator     | _%_                  | _%_                     |

Config: T=_, batch_size=_, data=_

**Reading:** _(fill in once real — which bucket dominates, does it clear the §2 60%/50%
thresholds)_

## 2. Sweep: T and batch size vs. SpikingJelly/PyTorch ratio

Model: mlp. Full grid in `bench/results/mlp_sweep.csv` (generated by `sweep.py`).

| T   | batch_size | PyTorch wall_s | SJ wall_s | SJ speedup | PyTorch launch_gap_frac | SJ launch_gap_frac |
|-----|-----------|-----------------|-----------|------------|--------------------------|----------------------|
|     |           |                 |           |            |                          |                      |

**Reading:** _(fill in — does the ratio move toward SJ as T grows / batch shrinks, as thesis 1
predicts? at what point does GEMM dominate enough that the gap stops mattering?)_

## 3. Conv model

Same two tables as above, model=conv.

## 4. Recurrent (ALIF) model

Same two tables as above, model=rnn. Note: both `models.RecurrentSNN` and
`models_sj.RecurrentSNN_SJ` single-step through the recurrent connection (SpikingJelly has no
fused multi-step kernel for a recurrent weight matrix), so this comparison isolates the
neuron-update kernel cost from the "did we fuse the whole time loop" question answered by mlp/conv.

## 5. torch.compile baseline

What Inductor already fuses on the plain-PyTorch models, same configs as §1. This is the real
baseline to beat, not eager PyTorch.

## 6. Firing rates

Actual spike density observed during training, per model. Sparsity (thesis 4) doesn't pay on
GPU below ~1-5% density — record whether our rate-coded inputs land anywhere near that.

## Decision

_(fill in once §1–2 are real: does GEMM dominate (>60%, weak thesis, reframe around memory +
ergonomics) or does elementwise+launch dominate (>50%, thesis confirmed, §1.1/§1.3 are the
product)? Log the outcome in `DECISIONS.md`.)_
